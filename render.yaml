services:
  - type: web
    name: equation-sie-gamma
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```

Et le nouveau `requirements.txt` à uploader aussi :
```
flask==3.0.3
requests==2.31.0
pdfplumber==0.11.0
pypdf==4.2.0
Pillow==10.3.0
gunicorn==22.0.0