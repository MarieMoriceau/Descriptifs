services:
  - type: web
    name: equation-sie-gamma
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600 --workers 1 --worker-class gevent --worker-connections 100
